"""Core DeepSeek-V3.2 codegen pipeline.

This package is the Lambda-deployable unit. It depends only on the Python
standard library and boto3 (which is pre-installed in the Lambda runtime),
so it can be zipped and uploaded without bundling dependencies.
"""
